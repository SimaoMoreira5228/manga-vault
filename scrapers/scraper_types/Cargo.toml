[package]
name = "scraper_types"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
mlua = { version = "0.10", features = [
  "lua54",
  "serialize",
  "send",
  "vendored",
] }
