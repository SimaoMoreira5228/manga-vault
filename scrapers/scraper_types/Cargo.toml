[package]
name = "scraper_types"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { workspace = true }
mlua = { version = "0.11", features = [
  "lua54",
  "serialize",
  "send",
  "vendored",
] }
regex = "1.11"
chrono = { workspace = true }
